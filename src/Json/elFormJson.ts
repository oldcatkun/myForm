export interface elFormProps {
    inline?:boolean, //表单布局
    size?: 'large' | 'default' | 'small' //内部组件大小
    labelPosition?: 'left' | 'right' | 'top', //label位置
    labelWidth?: number | string, //label宽度
}

